# overflow

设置元素溢出时所需的行为

由 `overflow-x` `overflow-y` 两个属性构成

+ `visible` 内容不能被裁减并且可能渲染到边距盒（padding）的外部。
+ `hidden` 没有滚动条, 不允许用户滚动. 但是可以通过编程方式滚动, 因此元素仍然是一个滚动的容器
+ `clip` 同 `hidden`, 但是无法通过编程滚动
+ `scroll` 内容将被裁剪以适应边距, 无论是否实际裁剪了任何内容，浏览器总是显示滚动条，以防止滚动条在内容改变时出现或者消失
+ `auto`


