
# BFC

## 什么是BFC
> 块级格式化区域
+ FC是一个独立的布局环境，其中的元素布局是不受外界的影
  + 父元素的垂直外边距不会和子元素重叠
+ 并且在一个BFC中，块盒与行盒（行盒由一行中所有的内联元素所组成）都会垂直的沿着其父元素的边框排列。
+ 计算 BFC 的高度时，浮动子元素也参与计算
+ BFC 的区域不会与 float 的元素区域重叠

## 开启 BFC 的方法
+ 设置浮动
+ 设置元素的绝对定位
+ 设置为 inline-block
+ 将 overflow 设置为非 visible

