# btop

```shell
# 可以换成 github 源
git clone https://gitee.com/mirrors/btop.git

cd btop

make && make install


# (可选) 设置为root用户运行
make setuid

#######################
# 卸载
make uninstall
make clean
make distclean

```
